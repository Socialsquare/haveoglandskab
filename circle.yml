machine:
  node:
    version: 6.1.0
dependencies:
  override:
    - composer install
    - cd theme; npm install
test:
  override:
    - echo "No tests"
deployment:
  production:
    branch: production
    commands:
      - ./scripts/deploy.sh
