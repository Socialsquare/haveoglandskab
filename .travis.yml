dist: trusty
sudo: false
install:
  - nvm install -s
  - composer install
  - cd theme; npm install
script:
  - exit 0
deploy:
  provider: script
  script: scripts/deploy.sh
  on:
    branch: production
